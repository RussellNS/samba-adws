<s:Envelope xmlns:s="http://www.w3.org/2003/05/soap-envelope" xmlns:a="http://www.w3.org/2005/08/addressing">
    <s:Header>
        <a:Action s:mustUnderstand="1">{{ Action }}Response</a:Action>
        <a:RelatesTo>{{ MessageID }}</a:RelatesTo>
    </s:Header>
    <s:Body>
        <{{ ResponseTag }} xmlns="{{ Namespace }}">
            {% for obj in objects %}
            <addata:{{ obj.type }} xmlns:addata="http://schemas.microsoft.com/2008/1/ActiveDirectory/Data">
                {% for attr in obj.attrs %}
                {{ attr.to_xml()|safe }}
                {% endfor %}
            </addata:{{ obj.type }}>
            {% endfor %}
        </{{ ResponseTag }}>
    </s:Body>
</s:Envelope>
